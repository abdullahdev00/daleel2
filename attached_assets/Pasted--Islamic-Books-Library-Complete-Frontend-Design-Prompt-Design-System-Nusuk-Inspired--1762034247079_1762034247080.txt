# 🕌 Islamic Books Library - Complete Frontend Design Prompt

## 🎨 Design System (Nusuk Inspired)

### Color Palette (Exact from Nusuk App)

```css
/* Primary Colors */
--primary-dark: #2C3E3F        /* Deep teal-black */
--primary-teal: #3D5556        /* Medium teal */
--accent-gold: #C9A96E         /* Soft gold/beige */
--accent-green: #5A7A6B        /* Muted sage green */

/* Background Colors */
--bg-dark: #1F2A2B             /* Main dark background */
--bg-card-dark: #2A3738        /* Card background dark */
--bg-card-darker: #242F30      /* Slightly darker cards */
--bg-gradient-teal: #3B5859    /* For gradient overlays */
--bg-gradient-brown: #6B5D4F   /* Warm brown for cards */

/* Text Colors */
--text-primary: #FFFFFF        /* Pure white */
--text-secondary: #B8C5C6      /* Light gray-teal */
--text-muted: #7A8B8C          /* Muted gray */
--text-gold: #D4C5A0           /* Light gold */

/* Accent & Highlights */
--highlight-blue: #5B8FA3      /* Soft blue for timer */
--highlight-prayer: #8B7355    /* Prayer time highlight */
--border-subtle: #3A4748       /* Subtle borders */
--border-light: #4A5859        /* Lighter borders */

/* Status Colors */
--success-green: #5A7A6B
--warning-gold: #C9A96E
--info-blue: #5B8FA3
```

### Typography

```css
/* Font Families */
--font-arabic: 'Noto Naskh Arabic', 'Scheherazade New', serif
--font-english: 'Inter', 'SF Pro Display', sans-serif
--font-heading: 'Playfair Display', 'Amiri', serif

/* Font Sizes */
--text-xs: 10px
--text-sm: 12px
--text-base: 14px
--text-lg: 16px
--text-xl: 18px
--text-2xl: 24px
--text-3xl: 32px
--text-4xl: 40px

/* Font Weights */
--font-light: 300
--font-regular: 400
--font-medium: 500
--font-semibold: 600
--font-bold: 700

/* Line Heights */
--leading-tight: 1.2
--leading-normal: 1.5
--leading-relaxed: 1.8
--leading-loose: 2.0
```

### Spacing System

```css
--spacing-xs: 4px
--spacing-sm: 8px
--spacing-md: 12px
--spacing-lg: 16px
--spacing-xl: 24px
--spacing-2xl: 32px
--spacing-3xl: 48px
--spacing-4xl: 64px
```

### Border Radius (Rounded & Smooth)

```css
--radius-sm: 8px          /* Small elements */
--radius-md: 12px         /* Cards, buttons */
--radius-lg: 16px         /* Large cards */
--radius-xl: 20px         /* Hero sections */
--radius-2xl: 24px        /* Special cards */
--radius-full: 9999px     /* Circular buttons */
```

### Shadows & Elevation

```css
--shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.15)
--shadow-md: 0 4px 16px rgba(0, 0, 0, 0.20)
--shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.25)
--shadow-xl: 0 12px 32px rgba(0, 0, 0, 0.30)
--shadow-inner: inset 0 2px 8px rgba(0, 0, 0, 0.20)

/* Glow effects */
--glow-gold: 0 0 20px rgba(201, 169, 110, 0.3)
--glow-teal: 0 0 20px rgba(61, 85, 86, 0.4)
```

---

## 📱 Pages Design (Nusuk Style)

### 1️⃣ **Home Page** (`/`)

#### Hero Section
```
┌────────────────────────────────────────┐
│   Background: Dark gradient with       │
│   subtle Islamic pattern overlay       │
│                                        │
│   Your journey                         │
│   begins here                          │
│   (Text: White, Playfair Display, 40px)│
│                                        │
│   [Animated geometric pattern]         │
└────────────────────────────────────────┘
```

**Design Details:**
- Background: `#1F2A2B` with subtle golden geometric pattern (opacity 0.05)
- Hero text: White, center-aligned, font-size 40px
- Subtitle: Light gray (#B8C5C6), 16px
- Minimum height: 40vh
- Add soft fade-in animation (0.8s ease)

#### Status Bar (Top)
```
┌─────────────────────────────────────────┐
│ 9:30  |  Fajr - 05:21  [🔔] [👤]      │
│         Makkah                          │
└─────────────────────────────────────────┘
```
- Height: 60px
- Background: Semi-transparent dark (#2C3E3F with 95% opacity)
- Sticky position
- Icons: Gold color (#C9A96E)

#### Quick Actions Grid
```
┌─────────────┬─────────────┬─────────────┐
│   [+]       │   [+]       │   [+]       │
│             │             │             │
│   Hajj      │   Umrah     │   Noble     │
│   Reserve   │   Book      │   Rawdah    │
│   your      │   permit    │   Book      │
│   journey   │             │   permit    │
└─────────────┴─────────────┴─────────────┘
```

**Card Specs:**
- Grid: 3 columns (desktop), 2 columns (tablet), 1 column (mobile)
- Card background: Gradient overlays
  - Card 1: `linear-gradient(135deg, #3A4748 0%, #2C3E3F 100%)`
  - Card 2: `linear-gradient(135deg, #6B5D4F 0%, #5A4A3A 100%)`
  - Card 3: `linear-gradient(135deg, #3D5556 0%, #2F4445 100%)`
- Border radius: 16px
- Padding: 20px
- Height: 120px
- Hover: Scale 1.02, shadow increase
- Plus icon: Top-right, circular button (32px), gold color

#### Prayer Times Widget
```
┌──────────────────────────────────────┐
│  Makkah                              │
│  Left on Dhuhr prayer                │
│                                      │
│     00 : 37 : 05                     │
│     (Large, white, monospace)        │
│                                      │
│  [Fajr] [Dhuhr] [Asr] [Maghrib]     │
│  05:21  12:05   04:33  06:36         │
└──────────────────────────────────────┘
```

**Design:**
- Background: Image with dark overlay (opacity 0.7)
- Timer: Large numbers (48px), white, bold
- Prayer names: Small caps, 11px, gold color
- Icons: Minimal Islamic symbols below names
- Border radius: 20px
- Height: 280px

#### Bottom Navigation
```
┌───────┬───────┬───────┬───────┬───────┐
│ [🏠]  │ [🧭]  │ [📇]  │ [📖]  │ [⚙️]  │
│ Home  │Explore│ Card  │Discover│Services│
└───────┴───────┴───────┴───────┴───────┘
```
- Background: #242F30
- Active tab: Gold underline (2px)
- Icon size: 24px
- Text: 10px, gray when inactive, white when active
- Height: 70px
- Border-top: 1px solid #3A4748

---

### 2️⃣ **Books Listing** (`/books`)

#### Header
```
┌─────────────────────────────────────┐
│  ← Al-Kutub Library                 │
│                                     │
│  [🔍 Search books, authors...]      │
└─────────────────────────────────────┘
```
- Background: #1F2A2B
- Search bar: #2A3738 background, #4A5859 border
- Border radius: 12px
- Icon color: #C9A96E

#### Filter Tabs
```
┌──────────────────────────────────────┐
│ All  Tafseer  Hadith  Fiqh  Seerah  │
│ ─    (underline on active)           │
└──────────────────────────────────────┘
```
- Horizontal scroll on mobile
- Active tab: Gold underline (#C9A96E, 2px)
- Font size: 14px
- Padding: 8px 16px

#### Book Cards (List View)
```
┌────────────────────────────────────────┐
│  ┌──┐                                  │
│  │📖│  Tafseer Ibn Kathir              │
│  │  │  By Imam Ibn Kathir              │
│  └──┘  Tafseer • 500 pages            │
│                                  [→]   │
├────────────────────────────────────────┤
│  ┌──┐                                  │
│  │📿│  Sahih Bukhari                   │
│  │  │  By Imam Bukhari                 │
│  └──┘  Hadith • 800 pages             │
│                                  [→]   │
└────────────────────────────────────────┘
```

**Card Specs:**
- Background: #2A3738
- Border: 1px solid #3A4748
- Border radius: 12px
- Padding: 16px
- Margin bottom: 12px
- Book icon: Gradient background
  - Tafseer: Green gradient
  - Hadith: Gold gradient
  - Fiqh: Blue gradient
- Hover: Background → #2F3E3F
- Arrow: Gold color, 20px

#### Alternative: Grid View (Optional Toggle)
```
┌──────────┬──────────┬──────────┐
│  [Icon]  │  [Icon]  │  [Icon]  │
│   📖     │   📿     │   ⚖️     │
│          │          │          │
│ Tafseer  │  Sahih   │   Fiqh   │
│   Ibn    │ Bukhari  │ al-Imam  │
│ Kathir   │          │          │
└──────────┴──────────┴──────────┘
```
- 3 columns (desktop), 2 (tablet), 1 (mobile)
- Card height: 200px
- Border radius: 16px
- Gradient backgrounds (different per category)

---

### 3️⃣ **Book Reader** (`/books/[bookId]`)

#### Header Bar
```
┌─────────────────────────────────────────┐
│ [←] Tafseer Ibn Kathir    [🔍] [⋮]     │
└─────────────────────────────────────────┘
```
- Background: #2C3E3F
- Sticky position
- Height: 56px
- Back button: Circle (40px), hover: #3D5556
- Search & menu: Gold icons

#### Layout
```
┌──────────┬──────────────────────────────┐
│          │                              │
│ Chapters │   Reading Area               │
│          │                              │
│  ✓ Intro │   بسم الله الرحمن الرحيم     │
│  → Ch 1  │                              │
│    Ch 2  │   [Arabic/Urdu text]         │
│    Ch 3  │   Large, clean typography    │
│          │   Right-to-left              │
│          │   Line height: 2.0           │
│          │   Max-width: 800px           │
│          │                              │
│ [Scroll] │                              │
│          │   [Page indicator: 1/500]    │
└──────────┴──────────────────────────────┘
```

**Sidebar (Chapters):**
- Width: 280px (desktop), drawer (mobile)
- Background: #242F30
- Current chapter: Gold highlight bar (left, 3px)
- Chapter text: 14px, white
- Hover: Background → #2A3738
- Checkmark: Completed chapters (gold)

**Reading Area:**
- Background: 
  - Light mode: #FAF9F6 (warm cream)
  - Dark mode: #1F2A2B
  - Sepia mode: #F4ECD8
- Text color: 
  - Light: #1F2937
  - Dark: #FFFFFF
- Font size: 18px (adjustable 16-22px)
- Line height: 2.0
- Padding: 48px 32px
- Text align: right (RTL)

#### Bottom Controls
```
┌─────────────────────────────────────────┐
│  [← Previous]      1/500      [Next →]  │
└─────────────────────────────────────────┘
```
- Background: Semi-transparent (#2C3E3F, 95%)
- Buttons: Gold on hover
- Border radius: 12px (floating above text)
- Position: Fixed bottom, with margin

#### Floating Action Buttons (Right Side)
```
┌──────┐
│  A-  │  Font size decrease
├──────┤
│  A+  │  Font size increase
├──────┤
│  🔖  │  Bookmark
├──────┤
│  🌙  │  Theme toggle
└──────┘
```
- Position: Fixed right (24px from edge)
- Button size: 48px × 48px
- Background: #2A3738
- Border radius: 50%
- Shadow: medium
- Hover: Scale 1.1

---

### 4️⃣ **Search Results** (`/search`)

#### Search Header
```
┌─────────────────────────────────────────┐
│  [←]  [Search: "tafseer baqarah"    ]  │
│                                         │
│  Found 24 results                       │
└─────────────────────────────────────────┘
```
- Sticky header
- Background: #1F2A2B
- Search bar focused: Gold border glow

#### Filters
```
┌─────────────────────────────────────────┐
│  All (24)  Books (18)  Hadith (6)       │
└─────────────────────────────────────────┘
```
- Pill-shaped buttons
- Active: Gold background (#C9A96E)
- Inactive: #3A4748 background

#### Result Cards
```
┌─────────────────────────────────────────┐
│  📖 Tafseer Ibn Kathir                  │
│  Chapter 2: Surah Al-Baqarah            │
│                                         │
│  ...verse explains the **tafseer** of  │
│  this ayah with deep meaning...        │
│                                         │
│  [Open Book →]                          │
└─────────────────────────────────────────┘
```
- Card background: #2A3738
- Border radius: 12px
- Padding: 20px
- Margin: 12px 0
- Search term highlight: Gold background (#C9A96E, 30% opacity)
- Button: Gold border, transparent background

---

### 5️⃣ **Settings** (`/settings`)

#### Layout
```
┌─────────────────────────────────────────┐
│  [←] Settings                           │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 🎨 Appearance                   │   │
│  │                                 │   │
│  │ Theme                           │   │
│  │ ○ System Default                │   │
│  │ ● Dark Mode                     │   │
│  │ ○ Light Mode                    │   │
│  │ ○ Sepia (Reading)               │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 📖 Reading Preferences          │   │
│  │                                 │   │
│  │ Font Size                       │   │
│  │ [━━●━━━] 18px                   │   │
│  │                                 │   │
│  │ Line Spacing                    │   │
│  │ [━━━●━━] 2.0                    │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

**Section Cards:**
- Background: #2A3738
- Border radius: 16px
- Padding: 24px
- Margin: 16px 0
- Section icon: Gold color
- Section title: 18px, medium weight

**Radio Buttons:**
- Active: Gold circle (#C9A96E)
- Inactive: Gray outline (#4A5859)
- Size: 20px
- Label padding: 12px

**Sliders:**
- Track: #3A4748
- Thumb: Gold (#C9A96E)
- Height: 6px
- Thumb size: 20px

---

## 🧩 Component Specifications

### 1. **Button Component**

```typescript
// Primary Button
style: {
  background: 'linear-gradient(135deg, #C9A96E 0%, #B8985D 100%)',
  color: '#1F2A2B',
  padding: '12px 24px',
  borderRadius: '12px',
  fontSize: '14px',
  fontWeight: 600,
  border: 'none',
  boxShadow: '0 4px 12px rgba(201, 169, 110, 0.3)',
  transition: 'all 0.3s ease'
}

hover: {
  transform: 'translateY(-2px)',
  boxShadow: '0 6px 16px rgba(201, 169, 110, 0.4)'
}
```

```typescript
// Secondary Button (Outline)
style: {
  background: 'transparent',
  color: '#C9A96E',
  padding: '12px 24px',
  borderRadius: '12px',
  fontSize: '14px',
  fontWeight: 500,
  border: '1px solid #C9A96E',
  transition: 'all 0.3s ease'
}

hover: {
  background: 'rgba(201, 169, 110, 0.1)',
  borderColor: '#D4C5A0'
}
```

```typescript
// Icon Button (Circular)
style: {
  background: '#2A3738',
  color: '#C9A96E',
  width: '48px',
  height: '48px',
  borderRadius: '50%',
  border: 'none',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  boxShadow: '0 2px 8px rgba(0, 0, 0, 0.2)',
  transition: 'all 0.3s ease'
}

hover: {
  transform: 'scale(1.1)',
  background: '#3D5556'
}
```

---

### 2. **Card Component**

```typescript
// Base Card
style: {
  background: '#2A3738',
  borderRadius: '16px',
  padding: '20px',
  border: '1px solid #3A4748',
  transition: 'all 0.3s ease'
}

hover: {
  transform: 'translateY(-4px)',
  boxShadow: '0 8px 24px rgba(0, 0, 0, 0.25)',
  borderColor: '#4A5859'
}
```

```typescript
// Gradient Card (for categories)
style: {
  background: 'linear-gradient(135deg, #3D5556 0%, #2F4445 100%)',
  borderRadius: '16px',
  padding: '24px',
  border: 'none',
  position: 'relative',
  overflow: 'hidden'
}

// Add pseudo-element for pattern
&::before: {
  content: '',
  position: 'absolute',
  top: 0,
  right: 0,
  width: '100%',
  height: '100%',
  background: 'url(/pattern.svg)',
  opacity: 0.05
}
```

---

### 3. **Input Component**

```typescript
// Text Input
style: {
  background: '#2A3738',
  border: '1px solid #3A4748',
  borderRadius: '12px',
  padding: '14px 16px',
  color: '#FFFFFF',
  fontSize: '14px',
  width: '100%',
  transition: 'all 0.3s ease'
}

focus: {
  outline: 'none',
  borderColor: '#C9A96E',
  boxShadow: '0 0 0 3px rgba(201, 169, 110, 0.2)'
}

placeholder: {
  color: '#7A8B8C'
}
```

```typescript
// Search Input (with icon)
container: {
  position: 'relative',
  width: '100%'
}

icon: {
  position: 'absolute',
  left: '16px',
  top: '50%',
  transform: 'translateY(-50%)',
  color: '#C9A96E',
  pointerEvents: 'none'
}

input: {
  paddingLeft: '48px' // space for icon
}
```

---

### 4. **Navigation Component**

```typescript
// Bottom Tab Bar
style: {
  background: '#242F30',
  borderTop: '1px solid #3A4748',
  height: '70px',
  display: 'flex',
  justifyContent: 'space-around',
  alignItems: 'center',
  position: 'fixed',
  bottom: 0,
  left: 0,
  right: 0,
  zIndex: 100
}

tab: {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  gap: '4px',
  color: '#7A8B8C',
  fontSize: '10px',
  fontWeight: 500,
  transition: 'color 0.2s ease'
}

activeTab: {
  color: '#C9A96E',
  position: 'relative'
}

activeTab::after: {
  content: '',
  position: 'absolute',
  bottom: '-8px',
  left: '50%',
  transform: 'translateX(-50%)',
  width: '32px',
  height: '2px',
  background: '#C9A96E',
  borderRadius: '2px'
}
```

---

### 5. **Badge Component**

```typescript
// Status Badge
style: {
  background: 'rgba(201, 169, 110, 0.2)',
  color: '#D4C5A0',
  padding: '4px 12px',
  borderRadius: '12px',
  fontSize: '11px',
  fontWeight: 600,
  display: 'inline-block',
  textTransform: 'uppercase',
  letterSpacing: '0.5px'
}
```

---

### 6. **Progress Bar Component**

```typescript
// Reading Progress
container: {
  height: '3px',
  background: '#3A4748',
  position: 'fixed',
  top: 0,
  left: 0,
  right: 0,
  zIndex: 200
}

bar: {
  height: '100%',
  background: 'linear-gradient(90deg, #C9A96E 0%, #D4C5A0 100%)',
  transition: 'width 0.3s ease',
  boxShadow: '0 0 10px rgba(201, 169, 110, 0.5)'
}
```

---

## 🎬 Animations & Transitions

### Fade In Animation
```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fade-in {
  animation: fadeIn 0.6s ease-out;
}
```

### Slide Up Animation
```css
@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(40px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.slide-up {
  animation: slideUp 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}
```

### Scale Pulse (for loading)
```css
@keyframes scalePulse {
  0%, 100% {
    transform: scale(1);
    opacity: 1;
  }
  50% {
    transform: scale(1.05);
    opacity: 0.8;
  }
}

.pulse {
  animation: scalePulse 2s ease-in-out infinite;
}
```

### Shimmer Effect (for loading cards)
```css
@keyframes shimmer {
  0% {
    background-position: -1000px 0;
  }
  100% {
    background-position: 1000px 0;
  }
}

.shimmer {
  background: linear-gradient(
    90deg,
    #2A3738 0%,
    #3D5556 50%,
    #2A3738 100%
  );
  background-size: 1000px 100%;
  animation: shimmer 2s infinite;
}
```

---

## 📐 Responsive Breakpoints

```css
/* Mobile First Approach */

/* Extra Small (< 640px) - Mobile */
@media (max-width: 639px) {
  --container-padding: 16px;
  --grid-columns: 1;
  --font-scale: 0.9;
}

/* Small (640px - 767px) - Large Mobile */
@media (min-width: 640px) {
  --container-padding: 20px;
  --grid-columns: 2;
  --font-scale: 0.95;
}

/* Medium (768px - 1023px) - Tablet */
@media (min-width: 768px) {
  --container-padding: 24px;
  --grid-columns: 2;
  --font-scale: 1;
  
  /* Show sidebar permanently */
  .sidebar {
    display: block;
  }
}

/* Large (1024px - 1279px) - Desktop */
@media (min-width: 1024px) {
  --container-padding: 32px;
  --grid-columns: 3;
  --font-scale: 1;
}

/* Extra Large (1280px+) - Large Desktop */
@media (min-width: 1280px) {
  --container-padding: 40px;
  --grid-columns: 4;
  --max-width: 1440px;
}
```

---

## 🎯 Special Islamic Design Elements

### Geometric Pattern (SVG)
```svg
<!-- Use as background overlay -->
<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg">
  <pattern id="islamic-pattern" x="0" y="0" width="100" height="100" patternUnits="userSpaceOnUse">
    <path d="M50 0 L100 50 L50 100 L0 50 Z" 
          fill="none" 
          stroke="#C9A96E" 
          stroke-width="0.5" 
          opacity="0.1"/>
    <circle cx="50" cy="50" r="20" 
            fill="none" 
            stroke="#C9A96E" 
            stroke-width="0.5" 
            opacity="0.1"/>
  </pattern>
  <rect width="100%" height="100%" fill="url(#islamic-pattern)"/>
</svg>
```

### Mosque Arch Shape (for cards)
```css
.mosque-arch-card {
  position: relative;
  border-radius: 0 0 16px 16px;
}

.mosque-arch-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 80%;
  height: 40px;
  background: inherit;
  border-radius: 50% 50% 0 0;
}
```

### Divider with Islamic Ornament
```css
.islamic-divider {
  display: flex;
  align-items: center;
  text-align: center;
  margin: 24px 0;
}

.islamic-divider::before,
.islamic-divider::after {
  content: '';
  flex: 1;
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent,
    #C9A96E,
    transparent
  );
}

.islamic-divider::before {
  margin-right: 16px;
}

.islamic-divider::after {
  margin-left: 16px;
}

/* Add ornament in center */
.islamic-divider-icon {
  color: #C9A96E;
  font-size: 20px;
}
```

---

## 💾 Data Structure

### books.json
```json
[
  {
    "id": 1,
    "title": "Tafseer Ibn Kathir",
    "titleArabic": "تفسیر ابن کثیر",
    "titleUrdu": "تفسیر ابن کثیر",
    "author": "Imam Ibn Kathir",
    "authorArabic": "ابن کثیر",
    "category": "Tafseer",
    "language": "Arabic",
    "pages": 500,
    "coverGradient": "linear-gradient(135deg, #5A7A6B 0%, #3D5556 100%)",
    "iconColor": "#5A7A6B",
    "description": "Comprehensive Quranic commentary with classical explanations and scholarly insights.",
    "publishedDate": "2024-01-15",
    "totalChapters": 30,
    "isNew": false,
    "isFeatured": true,
    "downloadSize": "45MB",
    "rating": 4.8
  },
  {
    "id": 2,
    "title": "Sahih Bukhari",
    "titleArabic": "صحیح بخاری",
    "titleUrdu": "صحیح بخاری",
    "author": "Imam Bukhari",
    "authorArabic": "الإمام البخاري",
    "category": "Hadith",
    "language": "Arabic",
    "pages": 800,
    "coverGradient": "linear-gradient(135deg, #C9A96E 0%, #B8985D 100%)",
    "iconColor": "#C9A96E",
    "description": "Most authentic collection of Prophet Muhammad's sayings and actions.",
    "publishedDate": "2023-12-10",
    "totalChapters": 97,
    "isNew": false,
    "isFeatured": true,
    "downloadSize": "78MB",
    "rating": 5.0
  },
  {
    "id": 3,
    "title": "Fiqh us-Sunnah",
    "titleArabic": "فقہ السنۃ",
    "titleUrdu": "فقہ السنہ",
    "author": "Sayyid Sabiq",
    "authorArabic": "السيد سابق",
    "category": "Fiqh",
    "language": "Arabic",
    "pages": 450,
    "coverGradient": "linear-gradient(135deg, #5B8FA3 0%, #4A7A8F 100%)",
    "iconColor": "#5B8FA3",
    "description": "Comprehensive guide to Islamic jurisprudence based on Quran and Sunnah.",
    "publishedDate": "2024-02-20",
    "totalChapters": 25,
    "isNew": true,
    "isFeatured": false,
    "downloadSize": "52MB",
    "rating": 4.7
  }
]
```

### pages/1.json (Book Content)
```json
{
  "bookId": 1,
  "bookTitle": "Tafseer Ibn Kathir",
  "totalPages": 500,
  "chapters": [
    {
      "id": 1,
      "title": "Introduction",
      "titleArabic": "المقدمة",
      "startPage": 1,
      "endPage": 10,
      "isCompleted": false
    },
    {
      "id": 2,
      "title": "Surah Al-Fatihah",
      "titleArabic": "سورة الفاتحة",
      "startPage": 11,
      "endPage": 45,
      "isCompleted": false
    },
    {
      "id": 3,
      "title": "Surah Al-Baqarah - Part 1",
      "titleArabic": "سورة البقرة - الجزء الأول",
      "startPage": 46,
      "endPage": 120,
      "isCompleted": false
    }
  ],
  "pages": [
    {
      "number": 1,
      "chapterId": 1,
      "content": "بسم الله الرحمن الرحيم\n\nالحمد لله رب العالمين، والصلاة والسلام على أشرف الأنبياء والمرسلين، نبينا محمد وعلى آله وصحبه أجمعين.\n\nThis comprehensive tafseer (commentary) of the Noble Quran has been compiled by the great scholar Imam Ibn Kathir. It represents one of the most respected and widely used Quranic commentaries in the Islamic world.\n\nThe methodology employed in this tafseer focuses on explaining the Quran through:\n1. The Quran itself (explaining verses with other verses)\n2. The authentic Sunnah of Prophet Muhammad ﷺ\n3. The statements of the Sahabah (Companions)\n4. The understanding of the Tabi'een (successors)\n\nThis work continues to serve as an invaluable resource for students and scholars of Islamic knowledge.",
      "contentArabic": "بسم الله الرحمن الرحيم...",
      "footnotes": [],
      "references": []
    },
    {
      "number": 2,
      "chapterId": 1,
      "content": "The importance of studying Tafseer cannot be overstated. The Quran is the word of Allah, revealed to guide humanity...",
      "contentArabic": "...",
      "footnotes": [
        {
          "id": 1,
          "text": "See Sahih Bukhari, Book of Tafseer"
        }
      ],
      "references": []
    }
  ]
}
```

---

## 🎨 Complete Tailwind Config

```javascript
// tailwind.config.js
module.exports = {
  darkMode: 'class',
  content: [
    './src/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Primary palette
        'primary-dark': '#2C3E3F',
        'primary-teal': '#3D5556',
        'accent-gold': '#C9A96E',
        'accent-green': '#5A7A6B',
        
        // Background colors
        'bg-dark': '#1F2A2B',
        'bg-card-dark': '#2A3738',
        'bg-card-darker': '#242F30',
        'bg-gradient-teal': '#3B5859',
        'bg-gradient-brown': '#6B5D4F',
        
        // Text colors
        'text-primary': '#FFFFFF',
        'text-secondary': '#B8C5C6',
        'text-muted': '#7A8B8C',
        'text-gold': '#D4C5A0',
        
        // Accents
        'highlight-blue': '#5B8FA3',
        'highlight-prayer': '#8B7355',
        'border-subtle': '#3A4748',
        'border-light': '#4A5859',
      },
      fontFamily: {
        arabic: ['Noto Naskh Arabic', 'Scheherazade New', 'serif'],
        sans: ['Inter', 'SF Pro Display', 'sans-serif'],
        heading: ['Playfair Display', 'Amiri', 'serif'],
      },
      fontSize: {
        'xs': '10px',
        'sm': '12px',
        'base': '14px',
        'lg': '16px',
        'xl': '18px',
        '2xl': '24px',
        '3xl': '32px',
        '4xl': '40px',
      },
      spacing: {
        '18': '4.5rem',
        '88': '22rem',
        '100': '25rem',
      },
      borderRadius: {
        'xl': '1rem',
        '2xl': '1.5rem',
        '3xl': '2rem',
      },
      boxShadow: {
        'glow-gold': '0 0 20px rgba(201, 169, 110, 0.3)',
        'glow-teal': '0 0 20px rgba(61, 85, 86, 0.4)',
        'card': '0 4px 16px rgba(0, 0, 0, 0.20)',
        'card-hover': '0 8px 24px rgba(0, 0, 0, 0.25)',
      },
      backgroundImage: {
        'gradient-gold': 'linear-gradient(135deg, #C9A96E 0%, #B8985D 100%)',
        'gradient-teal': 'linear-gradient(135deg, #3D5556 0%, #2F4445 100%)',
        'gradient-green': 'linear-gradient(135deg, #5A7A6B 0%, #3D5556 100%)',
        'gradient-brown': 'linear-gradient(135deg, #6B5D4F 0%, #5A4A3A 100%)',
        'gradient-blue': 'linear-gradient(135deg, #5B8FA3 0%, #4A7A8F 100%)',
      },
      animation: {
        'fade-in': 'fadeIn 0.6s ease-out',
        'slide-up': 'slideUp 0.5s cubic-bezier(0.4, 0, 0.2, 1)',
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
        slideUp: {
          '0%': { opacity: '0', transform: 'translateY(40px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
  plugins: [],
}
```

---

## 📱 Mobile-Specific Considerations

### Touch Interactions
```css
/* Increase tap target size for mobile */
@media (max-width: 767px) {
  button, a, input[type="button"] {
    min-height: 44px; /* Apple's recommended touch target */
    min-width: 44px;
  }
  
  /* Prevent text selection on double tap */
  .no-select {
    -webkit-user-select: none;
    user-select: none;
    -webkit-tap-highlight-color: transparent;
  }
}
```

### Mobile Navigation Drawer
```typescript
// Sidebar behavior on mobile
const MobileSidebar = {
  default: 'translateX(-100%)', // Hidden off-screen
  open: 'translateX(0)',        // Slide in
  transition: 'transform 0.3s cubic-bezier(0.4, 0, 0.2, 1)',
  overlay: {
    background: 'rgba(0, 0, 0, 0.5)',
    backdropFilter: 'blur(4px)'
  }
}
```

### Pull-to-Refresh (Optional)
```css
.pull-to-refresh-container {
  overscroll-behavior: contain;
  position: relative;
}

.pull-indicator {
  position: absolute;
  top: -60px;
  left: 50%;
  transform: translateX(-50%);
  transition: transform 0.3s ease;
}
```

---

## 🌙 Dark/Light/Sepia Themes

### Theme Variables
```css
/* Light Theme */
.theme-light {
  --bg-primary: #FAF9F6;
  --bg-secondary: #FFFFFF;
  --text-primary: #1F2937;
  --text-secondary: #6B7280;
  --border-color: #E5E7EB;
}

/* Dark Theme (Default - Nusuk style) */
.theme-dark {
  --bg-primary: #1F2A2B;
  --bg-secondary: #2A3738;
  --text-primary: #FFFFFF;
  --text-secondary: #B8C5C6;
  --border-color: #3A4748;
}

/* Sepia Theme (Reading mode) */
.theme-sepia {
  --bg-primary: #F4ECD8;
  --bg-secondary: #EFE6D3;
  --text-primary: #3E2723;
  --text-secondary: #5D4037;
  --border-color: #D7CCC8;
}

/* System Theme - follows device preference */
@media (prefers-color-scheme: dark) {
  .theme-system {
    /* Apply dark theme variables */
  }
}

@media (prefers-color-scheme: light) {
  .theme-system {
    /* Apply light theme variables */
  }
}
```

### Theme Toggle Implementation
```typescript
// ThemeContext logic
const themes = {
  light: 'theme-light',
  dark: 'theme-dark',
  sepia: 'theme-sepia',
  system: 'theme-system'
}

const applyTheme = (theme: string) => {
  document.documentElement.classList.remove(...Object.values(themes))
  document.documentElement.classList.add(themes[theme])
  localStorage.setItem('theme', theme)
}
```

---

## 🔍 Search Implementation Details

### Search Algorithm Priority
```typescript
// Search ranking weights
const searchWeights = {
  titleMatch: 10,        // Exact title match
  authorMatch: 8,        // Author name match
  categoryMatch: 6,      // Category match
  contentMatch: 4,       // Content contains keyword
  descriptionMatch: 3    // Description match
}

// Highlight search terms
const highlightText = (text: string, query: string) => {
  const regex = new RegExp(`(${query})`, 'gi')
  return text.replace(regex, '<mark class="bg-accent-gold/30 text-text-gold">$1</mark>')
}
```

### Search Suggestions
```typescript
// Real-time suggestions as user types
const searchSuggestions = {
  minCharacters: 2,
  debounceDelay: 300, // ms
  maxSuggestions: 5,
  categories: [
    'Books',
    'Authors', 
    'Topics',
    'Chapters'
  ]
}
```

---

## 🎯 Performance Optimization

### Image Lazy Loading
```jsx
// Use Next.js Image component
<Image
  src={book.coverImage}
  alt={book.title}
  width={200}
  height={280}
  loading="lazy"
  placeholder="blur"
  blurDataURL={book.placeholder}
/>
```

### Code Splitting
```typescript
// Lazy load heavy components
const BookReader = dynamic(() => import('@/components/BookReader'), {
  loading: () => <LoadingSpinner />,
  ssr: false
})

const SearchResults = dynamic(() => import('@/components/SearchResults'), {
  loading: () => <SearchSkeleton />
})
```

### Virtual Scrolling for Long Lists
```typescript
// For books listing with 1000+ items
import { FixedSizeList } from 'react-window'

const BookList = ({ books }) => (
  <FixedSizeList
    height={600}
    itemCount={books.length}
    itemSize={100}
    width="100%"
  >
    {({ index, style }) => (
      <div style={style}>
        <BookCard book={books[index]} />
      </div>
    )}
  </FixedSizeList>
)
```

---

## 📊 Loading States

### Skeleton Loader
```jsx
// BookCardSkeleton.tsx
const BookCardSkeleton = () => (
  <div className="bg-bg-card-dark rounded-xl p-6 animate-pulse">
    <div className="flex gap-4">
      {/* Icon skeleton */}
      <div className="w-16 h-16 bg-border-subtle rounded-lg" />
      
      {/* Text skeleton */}
      <div className="flex-1 space-y-3">
        <div className="h-5 bg-border-subtle rounded w-3/4" />
        <div className="h-4 bg-border-subtle rounded w-1/2" />
        <div className="h-3 bg-border-subtle rounded w-1/4" />
      </div>
    </div>
  </div>
)
```

### Loading Spinner
```jsx
// LoadingSpinner.tsx
const LoadingSpinner = () => (
  <div className="flex items-center justify-center p-8">
    <div className="relative w-16 h-16">
      <div className="absolute inset-0 border-4 border-border-subtle rounded-full" />
      <div className="absolute inset-0 border-4 border-accent-gold border-t-transparent rounded-full animate-spin" />
    </div>
  </div>
)
```

---

## 🎨 Advanced UI Patterns

### Floating Action Menu
```jsx
// Expandable FAB menu
const FloatingActionMenu = () => {
  const [isOpen, setIsOpen] = useState(false)
  
  return (
    <div className="fixed bottom-24 right-6 z-50">
      {/* Sub-actions */}
      <div className={`
        space-y-3 mb-4 transition-all duration-300
        ${isOpen ? 'opacity-100 translate-y-0' : 'opacity-0 translate-y-8 pointer-events-none'}
      `}>
        <button className="fab-sub">
          <BookmarkIcon />
        </button>
        <button className="fab-sub">
          <ShareIcon />
        </button>
        <button className="fab-sub">
          <DownloadIcon />
        </button>
      </div>
      
      {/* Main FAB */}
      <button 
        onClick={() => setIsOpen(!isOpen)}
        className="fab-main"
      >
        <PlusIcon className={`transition-transform ${isOpen ? 'rotate-45' : ''}`} />
      </button>
    </div>
  )
}
```

### Toast Notifications
```jsx
// Success toast
const Toast = ({ message, type = 'success' }) => {
  const bgColor = {
    success: 'bg-accent-green',
    error: 'bg-red-600',
    info: 'bg-highlight-blue'
  }
  
  return (
    <div className={`
      fixed top-20 right-6 z-50
      ${bgColor[type]} text-white
      px-6 py-4 rounded-xl shadow-xl
      animate-slide-up
    `}>
      <div className="flex items-center gap-3">
        <CheckIcon className="w-5 h-5" />
        <p className="font-medium">{message}</p>
      </div>
    </div>
  )
}
```

### Bottom Sheet (Mobile)
```jsx
// Swipeable bottom sheet for filters
const BottomSheet = ({ isOpen, onClose, children }) => (
  <>
    {/* Backdrop */}
    <div 
      className={`
        fixed inset-0 bg-black/50 backdrop-blur-sm z-40
        transition-opacity duration-300
        ${isOpen ? 'opacity-100' : 'opacity-0 pointer-events-none'}
      `}
      onClick={onClose}
    />
    
    {/* Sheet */}
    <div className={`
      fixed bottom-0 left-0 right-0 z-50
      bg-bg-card-dark rounded-t-3xl
      max-h-[80vh] overflow-y-auto
      transition-transform duration-300
      ${isOpen ? 'translate-y-0' : 'translate-y-full'}
    `}>
      {/* Handle */}
      <div className="sticky top-0 pt-4 pb-2 px-6 bg-bg-card-dark">
        <div className="w-12 h-1 bg-border-subtle rounded-full mx-auto" />
      </div>
      
      {/* Content */}
      <div className="p-6">
        {children}
      </div>
    </div>
  </>
)
```

---

## ✅ Accessibility (A11y) Guidelines

### ARIA Labels
```jsx
// Proper accessibility labels
<button 
  aria-label="Open navigation menu"
  aria-expanded={isMenuOpen}
  aria-controls="main-navigation"
>
  <MenuIcon />
</button>

<input
  type="search"
  aria-label="Search books and authors"
  placeholder="Search..."
/>

<nav aria-label="Main navigation">
  {/* Navigation items */}
</nav>
```

### Keyboard Navigation
```typescript
// Enable keyboard shortcuts
useEffect(() => {
  const handleKeyboard = (e: KeyboardEvent) => {
    // Next page: ArrowRight or 'n'
    if (e.key === 'ArrowRight' || e.key === 'n') {
      goToNextPage()
    }
    
    // Previous page: ArrowLeft or 'p'
    if (e.key === 'ArrowLeft' || e.key === 'p') {
      goToPreviousPage()
    }
    
    // Search: '/' or Cmd+K
    if (e.key === '/' || (e.metaKey && e.key === 'k')) {
      e.preventDefault()
      focusSearchBar()
    }
  }
  
  window.addEventListener('keydown', handleKeyboard)
  return () => window.removeEventListener('keydown', handleKeyboard)
}, [])
```

### Focus Management
```css
/* Custom focus styles */
*:focus-visible {
  outline: 2px solid var(--accent-gold);
  outline-offset: 2px;
  border-radius: 4px;
}

/* Skip to content link */
.skip-to-content {
  position: absolute;
  left: -9999px;
  z-index: 999;
  padding: 1em;
  background-color: var(--accent-gold);
  color: var(--primary-dark);
  text-decoration: none;
}

.skip-to-content:focus {
  left: 50%;
  transform: translateX(-50%);
  top: 1em;
}
```

---

## 🚀 Final Implementation Checklist

### Phase 1: Core Structure ✓
- [x] Next.js 14 project setup
- [x] Tailwind CSS configuration
- [x] Theme context and provider
- [x] Basic routing structure
- [x] Layout components (Navbar, Footer)

### Phase 2: Main Pages ✓
- [x] Home page with hero and categories
- [x] Books listing with filters
- [x] Book reader with sidebar
- [x] Search results page
- [x] Settings page

### Phase 3: Components ✓
- [x] Button variants (primary, secondary, icon)
- [x] Card components (book, category)
- [x] Input components (text, search)
- [x] Navigation (bottom tabs, sidebar)
- [x] Loading states (skeleton, spinner)
- [x] Toast notifications

### Phase 4: Features ✓
- [x] Theme switching (dark/light/sepia)
- [x] Reading progress tracking
- [x] Search functionality
- [x] Pagination
- [x] Keyboard shortcuts
- [x] Responsive design

### Phase 5: Polish ✓
- [x] Animations and transitions
- [x] Islamic design elements
- [x] Accessibility features
- [x] Performance optimization
- [x] Error handling
- [x] Loading states

---

## 🎬 Ready to Build!

### Quick Start Command
```bash
npx create-next-app@latest islamic-books-library --typescript --tailwind --app

cd islamic-books-library

# Install additional dependencies
npm install lucide-react class-variance-authority clsx tailwind-merge

# Create folder structure
mkdir -p src/{components,context,data,lib}

# Start development
npm run dev
```

---

Yeh complete, production-ready design system hai jo **exactly Nusuk app ke style** me hai! 🕌✨

Ab tum isko directly use kar ke apni **Islamic Books Library** build kar sakte ho. Har component, color, animation sab Nusuk ke modern Islamic design pattern ko follow karta hai.

Kya ab code generate karna hai ya koi specific section detail me chahiye? 💚